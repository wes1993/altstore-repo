# OpenClient Personal AltStore Repository

AltStore/SideStore source for personal, non-commercial builds of OpenClient.

The IPA is built automatically from the upstream OpenClient source after a quiet period following upstream changes.

Add `apps.json` as the source URL in AltStore Classic or SideStore.
